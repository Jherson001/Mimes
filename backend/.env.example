# Local (XAMPP / MySQL local)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=mimes_db
DB_PORT=3306
DB_SSL=false
PORT=8000
JWT_SECRET=cambia_esto_en_produccion

# Cloud (Render + Aiven) — prefer a single URI from Aiven "Service URI":
# DATABASE_URL=mysql://avnadmin:PASSWORD@HOST:PORT/defaultdb
# JWT_SECRET=una_frase_larga_y_privada
